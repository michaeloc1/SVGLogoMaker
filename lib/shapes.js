class Circle {
    constructor(text, textColor, shapeColor){
        this.text = text;
        this.textColor = textColor;
        this.shapeColor = shapeColor;
        this.test = "hello"
    }
    render(){
        return "trying to return a string"
    }
}

module.exports = Circle;